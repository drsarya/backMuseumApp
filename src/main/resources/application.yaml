server:
  servlet:
    context-path: /api
spring:
  liquibase:
    change-log: classpath*:migrations/master-changelog.yaml
    user: postgres
    url: jdbc:postgresql://localhost:5432/postgres
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 1111
